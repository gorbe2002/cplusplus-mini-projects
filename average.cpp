#include <iostream>
using namespace std;

// Define average() here:
double average(double num1, double num2) {
  return (num1 + num2) / 2;
}

int main() {
  
  cout << average(42.0, 24.0) << endl;
  cout << average(1.0, 2.0) << endl;
  
}
