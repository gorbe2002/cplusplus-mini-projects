#include <iostream>
#include <cmath>
using namespace std;

// Define tenth_power() here:
int tenth_power(int num) {
  return pow(num, 10);
}

int main() {
  
  cout << tenth_power(0) << endl;
  cout << tenth_power(1) << endl;
  cout << tenth_power(2) << endl;
  
}
